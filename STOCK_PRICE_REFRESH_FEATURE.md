# 股价定时刷新功能实现说明

## 功能概述
实现了每5秒自动刷新持仓股票价格的功能，支持A股和港股的批量查询。

## 后端实现

### 1. 新增批量查询接口
**文件**: `routes/invest_holding.go`

**接口**: `POST /api/stock/batch-prices`

**功能**: 批量查询多只股票的实时价格

**请求格式**:
```json
{
  "stocks": [
    {
      "code": "300308.SZ",
      "isHK": false
    },
    {
      "code": "00700.HK",
      "isHK": true
    }
  ]
}
```

**响应格式**:
```json
{
  "success": true,
  "data": [
    {
      "code": "300308.SZ",
      "price": 45.67,
      "isHK": false
    },
    {
      "code": "00700.HK",
      "price": 328.50,
      "isHK": true
    }
  ]
}
```

**实现要点**:
- 自动分离A股和港股代码
- A股使用东方财富选股接口批量查询（高效）
- 港股使用东方财富行情接口逐个查询
- 保持原始顺序返回结果

### 2. 路由注册
**文件**: `routes/routes.go`

添加了新路由：
```go
app.POST("/api/stock/batch-prices", BatchQueryStockPricesHandler)
```

## 前端实现

### 1. 状态管理
**文件**: `statics/html/zen_stock_holding.html`

新增状态字段：
```javascript
state = {
    // ... 原有字段
    refreshTimer: null,      // 定时刷新计时器
    isRefreshing: false      // 是否正在刷新中
}
```

### 2. 核心功能函数

#### `refreshStockPrices()`
- 批量查询所有持仓股票的最新价格
- 更新本地数据并重新渲染
- 显示刷新状态（刷新中/已更新/刷新失败）
- 防止重复刷新

#### `startAutoRefresh()`
- 启动定时刷新（每5秒）
- 立即执行一次刷新
- 记录计时器ID

#### `stopAutoRefresh()`
- 停止定时刷新
- 清除计时器

### 3. UI增强

#### 刷新状态指示器
位置：持仓明细表格标题栏

状态显示：
- 🔄 **实时刷新** - 正常状态
- ⏳ **刷新中...** - 正在查询价格
- ✅ **已更新** - 成功更新（2秒后恢复）
- ❌ **刷新失败** - 查询失败（3秒后恢复）

#### 手动刷新按钮
- 位置：持仓明细表格标题栏右侧
- 功能：立即触发一次价格刷新
- 样式：白色边框按钮，悬停时变为蓝色

### 4. 智能启停机制

#### 自动启动条件
1. 页面初始化时，如果有持仓数据
2. 添加第一只股票时

#### 自动停止条件
1. 删除最后一只股票时
2. 页面隐藏时（切换标签页/最小化窗口）

#### 自动恢复条件
1. 页面重新显示时（如果有持仓数据）

### 5. 页面可见性监听
```javascript
document.addEventListener('visibilitychange', () => {
    if (document.hidden) {
        stopAutoRefresh();  // 页面隐藏时停止
    } else {
        if (state.stocks.length > 0) {
            startAutoRefresh();  // 页面显示时恢复
        }
    }
});
```

## 使用说明

### 用户体验
1. **自动刷新**: 添加持仓后自动开始每5秒刷新价格
2. **手动刷新**: 点击"刷新"按钮立即更新价格
3. **状态提示**: 实时显示刷新状态
4. **智能省电**: 切换标签页时自动暂停刷新

### 性能优化
1. **批量查询**: 一次请求获取所有股票价格
2. **防重复**: 刷新中时阻止新的刷新请求
3. **智能暂停**: 页面不可见时停止刷新
4. **增量更新**: 只在价格变化时更新UI

### 数据持久化
- 刷新后的价格自动保存到 localStorage
- 页面刷新后保留最新价格

## 技术亮点

1. **混合市场支持**: 同时支持A股和港股价格查询
2. **批量优化**: A股使用批量接口，大幅提升效率
3. **视觉反馈**: 多种状态提示，用户体验友好
4. **智能控制**: 根据页面状态自动启停
5. **错误处理**: 完善的异常捕获和提示机制

## 测试建议

1. **功能测试**:
   - 添加A股和港股混合持仓
   - 观察价格是否每5秒更新
   - 点击手动刷新按钮
   - 删除所有持仓，确认刷新停止

2. **性能测试**:
   - 添加10+只股票，观察刷新速度
   - 切换标签页，确认刷新暂停
   - 长时间运行，检查内存泄漏

3. **异常测试**:
   - 断网情况下的错误提示
   - 无效股票代码的处理
   - 接口超时的处理

## 后续优化建议

1. **可配置刷新间隔**: 允许用户自定义刷新频率（3秒/5秒/10秒）
2. **价格变动提醒**: 价格涨跌超过阈值时高亮显示
3. **历史价格记录**: 记录价格变动历史，绘制分时图
4. **WebSocket支持**: 使用WebSocket实现真正的实时推送
5. **批量优化港股**: 寻找港股批量查询接口

## 注意事项

1. 东方财富接口有频率限制，建议不要设置过短的刷新间隔
2. 港股价格单位为港元，已在UI中标注
3. 价格数据仅供参考，实际交易以交易所报价为准
